After downloadind all the attachments:-
1.Keep all the files under same directory
2.Run the model.py
	It uses the train_sample dataset which is attached with email.
3.After running model.py file
	You could see the model results
 	A model.pkl(pickle file gets created)
4.Now, run the prediction.py file
	It uses test_new dataset(attached in mail) which is minimized version of the actual test dataset(Containing 100k entries)
	A test_final.csv gets created
5.test_final.csv is the desired result.